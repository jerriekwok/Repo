# 厨房模拟游戏数据库设计文档

## 1. 文档概述

### 1.1 文档目的
本文档描述厨房模拟游戏的数据结构和存储设计，包括数据模型、存储方式和数据管理策略，为游戏数据的设计和维护提供指导。

### 1.2 设计背景
厨房模拟游戏是一个基于Unity引擎开发的3D模拟游戏，主要使用ScriptableObject作为数据存储方式，结合PlayerPrefs保存玩家配置。游戏数据包括物品数据、配方数据、游戏配置等。

### 1.3 数据存储方式
- **ScriptableObject**: 用于存储游戏静态数据，如物品定义、配方等
- **PlayerPrefs**: 用于存储玩家配置和进度，如自定义键位、游戏设置等
- **内存数据结构**: 用于存储游戏运行时数据，如当前游戏状态、玩家数据等

## 2. 数据模型设计

### 2.1 厨房物品数据模型 (KitchenObjectSO)

**数据用途**: 定义游戏中的厨房物品，包括食材、餐具等

**存储方式**: ScriptableObject

**文件路径**: `Assets/ScriptableObjects/KitchenObjectSO/`

**字段设计**:

| 字段名 | 数据类型 | 描述 |
|--------|----------|------|
| prefab | Transform | 物品的预制体 |
| sprite | Sprite | 物品的2D图标 |
| objectName | string | 物品的名称 |

**代码实现**:
```csharp
[CreateAssetMenu()]
public class KitchenObjectSO : ScriptableObject
{
    public Transform prefab;
    public Sprite sprite;
    public string objectName;
}
```

### 2.2 配方数据模型 (RecipeSO)

**数据用途**: 定义游戏中的菜品配方，包括所需食材和成品

**存储方式**: ScriptableObject

**文件路径**: `Assets/ScriptableObjects/RecipeSO/`

**字段设计**:

| 字段名 | 数据类型 | 描述 |
|--------|----------|------|
| kitchenObjectSOList | List<KitchenObjectSO> | 配方所需的食材列表 |
| recipeName | string | 配方的名称 |
| outputKitchenObjectSO | KitchenObjectSO | 配方的成品（可选） |

**代码实现**:
```csharp
[CreateAssetMenu()]
public class RecipeSO : ScriptableObject
{
    public List<KitchenObjectSO> kitchenObjectSOList;
    public string recipeName;
    public KitchenObjectSO outputKitchenObjectSO; // 可选，用于表示成品
}
```

### 2.3 游戏配置数据模型

**数据用途**: 定义游戏的基本配置参数

**存储方式**: 脚本中的序列化字段

**说明**: 当前项目中没有使用GameConfigSO类，游戏配置参数直接在各脚本中通过SerializeField定义

### 2.4 玩家配置数据模型

**数据用途**: 存储玩家的个性化配置

**存储方式**: PlayerPrefs

**说明**: 当前项目中没有使用PlayerConfigManager类，键位绑定通过GameInput类直接管理

### 2.5 游戏进度数据模型

**说明**: 当前项目中没有实现游戏进度管理功能，如分数、成就等

## 3. 数据关系设计

### 3.1 物品与配方关系

**关系类型**: 多对多关系

**描述**: 一个配方可以包含多个物品（食材），一个物品可以用于多个配方

**实现方式**: 在RecipeSO中使用List<KitchenObjectSO>存储所需食材

```csharp
public class RecipeSO : ScriptableObject
{
    public List<KitchenObjectSO> kitchenObjectSOList; // 配方所需的食材列表
    public string recipeName;
}
```

### 3.2 餐盘与食材关系

**关系类型**: 一对多关系

**描述**: 一个餐盘可以包含多个食材，一个食材只能属于一个餐盘

**实现方式**: 在PlateKitchenObject中使用List<KitchenObjectSO>存储添加的食材

```csharp
public class PlateKitchenObject : KitchenObject
{
    [SerializeField] private List<KitchenObjectSO> validKitchenObjectSOList;
    private List<KitchenObjectSO> kitchenObjectSOList; // 餐盘上的食材列表
    
    // 添加食材方法
    public bool TryAddIngredient(KitchenObjectSO kitchenObjectSO)
    {
        // 验证食材有效性
        if (!validKitchenObjectSOList.Contains(kitchenObjectSO))
        {
            return false;
        }
        
        // 验证是否已添加
        if (kitchenObjectSOList.Contains(kitchenObjectSO))
        {
            return false;
        }
        
        // 添加食材
        kitchenObjectSOList.Add(kitchenObjectSO);
        return true;
    }
}
```

## 4. 数据管理策略

### 4.1 ScriptableObject管理

**创建与编辑**:
- 通过Unity编辑器创建ScriptableObject实例
- 使用自定义编辑器扩展提升编辑体验

**加载与访问**:
- 通过Resources.Load()或直接引用加载ScriptableObject
- 使用单例模式管理全局数据访问

**版本控制**:
- ScriptableObject作为资源文件，通过Git进行版本控制
- 确保团队成员使用相同版本的数据

### 4.2 PlayerPrefs管理

**数据加密**:
- 对敏感数据进行简单加密，防止玩家修改
- 示例加密方法:

```csharp
private string Encrypt(string plainText)
{
    // 简单的XOR加密
    char[] chars = plainText.ToCharArray();
    for (int i = 0; i < chars.Length; i++)
    {
        chars[i] = (char)(chars[i] ^ 0xAA);
    }
    return new string(chars);
}

private string Decrypt(string cipherText)
{
    return Encrypt(cipherText); // XOR加密是对称的
}
```

**数据备份**:
- 定期备份PlayerPrefs数据
- 提供数据恢复功能

### 4.3 内存数据管理

**对象池技术**:
- 使用对象池管理厨房物品的创建和销毁
- 减少内存分配和垃圾回收

```csharp
public class KitchenObjectPool : MonoBehaviour
{
    [SerializeField] private KitchenObject kitchenObjectPrefab;
    private Queue<KitchenObject> kitchenObjectQueue = new Queue<KitchenObject>();
    
    // 获取对象
    public KitchenObject GetKitchenObject()
    {
        if (kitchenObjectQueue.Count == 0)
        {
            KitchenObject kitchenObject = Instantiate(kitchenObjectPrefab);
            return kitchenObject;
        }
        else
        {
            KitchenObject kitchenObject = kitchenObjectQueue.Dequeue();
            kitchenObject.gameObject.SetActive(true);
            return kitchenObject;
        }
    }
    
    // 回收对象
    public void ReturnKitchenObject(KitchenObject kitchenObject)
    {
        kitchenObject.gameObject.SetActive(false);
        kitchenObjectQueue.Enqueue(kitchenObject);
    }
}
```

**数据清理**:
- 在游戏状态切换时清理不需要的内存数据
- 确保资源及时释放

## 5. 数据访问接口设计

### 5.1 ScriptableObject访问接口

```csharp
public class KitchenObjectSOManager : MonoBehaviour
{
    public static KitchenObjectSOManager Instance { get; private set; }
    
    [SerializeField] private List<KitchenObjectSO> kitchenObjectSOList;
    [SerializeField] private List<RecipeSO> recipeSOList;
    
    private void Awake()
    {
        Instance = this;
    }
    
    // 获取所有物品
    public List<KitchenObjectSO> GetAllKitchenObjectSO()
    {
        return kitchenObjectSOList;
    }
    
    // 根据名称获取物品
    public KitchenObjectSO GetKitchenObjectSOByName(string objectName)
    {
        foreach (KitchenObjectSO kitchenObjectSO in kitchenObjectSOList)
        {
            if (kitchenObjectSO.objectName == objectName)
            {
                return kitchenObjectSO;
            }
        }
        return null;
    }
    
    // 获取所有配方
    public List<RecipeSO> GetAllRecipeSO()
    {
        return recipeSOList;
    }
    
    // 根据食材列表获取匹配的配方
    public RecipeSO GetRecipeSOFromKitchenObjectSOList(List<KitchenObjectSO> kitchenObjectSOList)
    {
        foreach (RecipeSO recipeSO in recipeSOList)
        {
            bool matchesRecipe = true;
            
            // 检查配方所需的所有食材
            foreach (KitchenObjectSO recipeKitchenObjectSO in recipeSO.kitchenObjectSOList)
            {
                bool found = false;
                
                // 检查当前列表是否包含配方所需的食材
                foreach (KitchenObjectSO kitchenObjectSO in kitchenObjectSOList)
                {
                    if (kitchenObjectSO == recipeKitchenObjectSO)
                    {
                        found = true;
                        break;
                    }
                }
                
                if (!found)
                {
                    matchesRecipe = false;
                    break;
                }
            }
            
            if (matchesRecipe)
            {
                return recipeSO;
            }
        }
        
        return null; // 没有匹配的配方
    }
}
```

### 5.2 PlayerPrefs访问接口

```csharp
public class PlayerPrefsManager : MonoBehaviour
{
    // 键位绑定管理
    public void SaveKeyBinding(GameInput.Binding binding, string newBinding)
    {
        string key = GetKeyBindingPlayerPrefsKey(binding);
        PlayerPrefs.SetString(key, newBinding);
        PlayerPrefs.Save();
    }
    
    public string LoadKeyBinding(GameInput.Binding binding)
    {
        string key = GetKeyBindingPlayerPrefsKey(binding);
        string defaultBinding = GameInput.Instance.GetDefaultBinding(binding);
        return PlayerPrefs.GetString(key, defaultBinding);
    }
    
    private string GetKeyBindingPlayerPrefsKey(GameInput.Binding binding)
    {
        return "KeyBinding_" + binding.ToString();
    }
    
    // 其他PlayerPrefs管理方法...
}
```

## 6. 数据扩展性设计

### 6.1 新物品添加流程

1. 创建新的KitchenObjectSO实例
2. 配置物品属性（预制体、名称、精灵）
3. 将物品添加到KitchenObjectSOManager的列表中
4. 在游戏中使用

### 6.2 新配方添加流程

1. 创建新的RecipeSO实例
2. 配置配方名称和所需食材
3. 将配方添加到RecipeSOManager的列表中
4. 在游戏中使用

### 6.3 新数据模型添加流程

1. 创建新的ScriptableObject类
2. 定义数据字段
3. 创建实例并配置数据
4. 添加访问接口
5. 在游戏中使用

## 7. 数据安全设计

### 7.1 PlayerPrefs数据保护

- 使用简单加密算法保护敏感数据
- 验证数据完整性，防止篡改

### 7.2 数据备份与恢复

- 提供数据备份功能
- 支持数据恢复到之前的状态

### 7.3 错误处理

- 处理数据加载失败的情况
- 提供默认数据作为备选

```csharp
public KitchenObjectSO GetKitchenObjectSOByName(string objectName)
{
    foreach (KitchenObjectSO kitchenObjectSO in kitchenObjectSOList)
    {
        if (kitchenObjectSO.objectName == objectName)
        {
            return kitchenObjectSO;
        }
    }
    
    // 如果找不到，返回默认物品
    Debug.LogWarning("KitchenObjectSO not found: " + objectName);
    return kitchenObjectSOList[0]; // 返回第一个物品作为默认值
}
```

## 8. 性能优化设计

### 8.1 数据加载优化

- 使用Resources.LoadAsync()异步加载大型数据
- 预加载常用数据，减少运行时加载时间

### 8.2 内存使用优化

- 使用对象池管理频繁创建和销毁的对象
- 及时清理不再使用的数据

### 8.3 数据访问优化

- 缓存常用数据，减少重复查询
- 使用索引提高查询效率

## 9. 附录

### 9.1 ScriptableObject创建指南

1. 在Unity编辑器中，右键点击Project窗口
2. 选择"Create" -> "KitchenObjectSO"（或其他自定义类型）
3. 配置新创建的ScriptableObject实例
4. 保存并使用

### 9.2 PlayerPrefs调试工具

```csharp
public class PlayerPrefsDebugTool : MonoBehaviour
{
    [ContextMenu("Clear All PlayerPrefs")]
    private void ClearAllPlayerPrefs()
    {
        PlayerPrefs.DeleteAll();
        Debug.Log("All PlayerPrefs cleared!");
    }
    
    [ContextMenu("Print All PlayerPrefs")]
    private void PrintAllPlayerPrefs()
    {
        Debug.Log("PlayerPrefs:");
        foreach (string key in PlayerPrefs.GetString("AllKeys").Split(','))
        {
            if (!string.IsNullOrEmpty(key))
            {
                Debug.Log(key + ": " + PlayerPrefs.GetString(key));
            }
        }
    }
}
```

### 9.3 数据迁移策略

当数据模型发生变化时，需要进行数据迁移：

1. 版本化数据模型
2. 实现数据迁移脚本
3. 在游戏启动时检查数据版本并执行迁移

```csharp
public class DataMigrationManager : MonoBehaviour
{
    private const string PLAYER_PREFS_DATA_VERSION = "DataVersion";
    private const int CURRENT_DATA_VERSION = 1;
    
    private void Awake()
    {
        int savedDataVersion = PlayerPrefs.GetInt(PLAYER_PREFS_DATA_VERSION, 0);
        
        if (savedDataVersion < CURRENT_DATA_VERSION)
        {
            // 执行数据迁移
            MigrateData(savedDataVersion, CURRENT_DATA_VERSION);
            
            // 更新数据版本
            PlayerPrefs.SetInt(PLAYER_PREFS_DATA_VERSION, CURRENT_DATA_VERSION);
            PlayerPrefs.Save();
        }
    }
    
    private void MigrateData(int fromVersion, int toVersion)
    {
        if (fromVersion == 0 && toVersion >= 1)
        {
            // 从版本0迁移到版本1
            MigrateFromVersion0To1();
        }
        
        // 其他迁移步骤...
    }
    
    private void MigrateFromVersion0To1()
    {
        // 迁移逻辑...
        Debug.Log("Data migrated from version 0 to 1");
    }
}
```

---

**版本**: 1.0.0
**发布日期**: 2025-12-06
